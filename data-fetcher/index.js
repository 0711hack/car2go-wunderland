const request = require('request');

const getLoginToken = () => {
  'use strict';
  
};

const fetchVehicles = auth => {
  'use strict';
  
};

const parsePositions = vehicles => {
  'use strict';
  
};

getLoginToken()
  .then(fetchVehicles)
  .then(parsePositions)
  .then(console.log);

